'use strict';

export class program {
    constructor(
        schoolName, city, country, schoolPic, mapPic, programName,
        programLen, programDesc, hasCoop, admissionCourses, admissionAvg,
        domesticTuition, internationalTuition, academicCostDetails, academicCosts,
        livingCostDetails, livingCosts, interestingFacts, primarySource1,
        primarySource2, primarySource3, secondarySource1, secondarySource2
    ) {
        
    }
}
